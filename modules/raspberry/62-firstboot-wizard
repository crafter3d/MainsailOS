#!/bin/bash
set -xe
export LC_ALL=C

########################################
# Functions and Basic Configuration    #
########################################

# Load common functions and set up cleanup trap
source /common.sh
install_cleanup_trap

# Load additional configuration (e.g., BASE_USER)
source /files/00-config

########################################
# First-boot wizard DISABLED           #
#                                      #
# SSH and KlipperScreen are now        #
# enabled at build time, default creds #
# are baked in (crafter3d/crafter3d),  #
# so the interactive wizard is no      #
# longer needed.                       #
########################################

echo "First-boot wizard skipped — SSH & KlipperScreen enabled at build time."
